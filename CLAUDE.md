# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

**WeCare** — a German/Austrian-market digital health platform for medical cannabis, built **problem-first** (Sleep, Pain, Stress & Anxiety, Migraine). The doctor / prescription / pharmacy layer is required but sits *behind* the assessment flow, never in front of it.

This codebase was repurposed from an unrelated Figma Make export ("Visualize Code", an hours-tracking demo). That demo's code is being stripped out phase by phase; its *engineering substrate* (Vite + React + Tailwind v4 + shadcn/ui) is being kept and reskinned. The rebuild spec lives at `../Downloads/WeCare_CLI_Implementation_Prompt.md` — it is the source of truth for scope, IA, copy rules, and the design system.

Git repo (initialised Aug 2026). Default branch `master`; put feature work on a branch and only commit / push when asked.

## Commands

```
pnpm install     # native build scripts (esbuild, @tailwindcss/oxide) are allowlisted in package.json > pnpm.onlyBuiltDependencies
pnpm dev         # Vite dev server on :5173
pnpm build       # vite build
pnpm typecheck   # tsc --noEmit  (strict; keep this green)
```

`pnpm build` does **not** type-check — run `pnpm typecheck` separately.

## Hard rules (from the spec — do not violate)

- No leaf/smoke/dispensary imagery or recreational language anywhere.
- Homepage and problem pages lead with the **problem**, never "prescription" / "treatment". The medical layer appears *after* the assessment as "medical review" / "prescription if medically appropriate" — never guaranteed.
- Never imply every user gets a prescription.
- Primary nav: **Sleep · Pain · Stress & Anxiety · Migraine** + (right) Login · Start Assessment. **No "Shop" / "Products" / "Flowers" / "Vapes" in primary nav.** **"How It Works" and "FAQ" were deliberately removed from `PRIMARY_NAV` (owner decision, Aug 2026) — do NOT re-add them.** How-It-Works is a homepage section (now just *before* the final CTA), and `/how-it-works` **redirects** to it (`/#how-it-works`; `ScrollToHash` in `RootLayout` does the scroll). The `/faq` page **has real content** (`src/pages/FaqPage.tsx`, `faq` i18n namespace, categorised service FAQ) but is linked only from the footer, not the primary nav. This intentionally overrides the spec's §17 nav list.
- The user-facing product layer is the **5 named Solutions** (abstract wellness names), never strain/format names up front. A **new** user (assessment q5 = "No, I am new") is **never** led with the stronger (higher-THC) option regardless of severity — `gentleFirst` wins, and the result-page nudge frames oil as the controlled starting format before flower/vape.
- Every new or touched string ships in **both** DE and EN via react-i18next, including dynamic content. Variables go *inside* translation strings, not concatenated around them.

## Architecture

### Styling & tokens — `src/styles/`  (WECARE BRAND BOOK 2026)
- **`index.css` is the whole design system** — it imports `fonts.css`, imports Tailwind, then holds `@theme static` (brand primitives), `:root` (semantic layer, `color-scheme: light`), `.dark` (the real dark appearance — see below) and `@layer base`. `theme.css` / `tailwind.css` no longer exist.
- **Color** (brand book): Light Azure `#f9fdfe` · **Azure `#218390`** (primary — the printed `#2d190e` is a typo) · **Dark Azure `#0d444b`** · Light Green `#e8f4ed`. Exposed as: `petrol-*` = the Azure teal ramp (brand / trust / medical UI + CTA), `sage-*` = the Light Green ramp (secondary / progress / "answered"), `danger-*` = a functional error red **only** (not a brand colour). Brand-name aliases: `--color-azure`, `--color-dark-azure`, `--color-light-azure`, `--color-light-green`. There is **no warm/amber accent** — the brand colour is the CTA (`--cta` = Azure, `--cta-hover` = Dark Azure).
- `:root` maps shadcn's `--primary`/`--card`/`--muted`/… onto the palette so vendored `ui/*` inherit it. `@theme inline` bridges to utilities.
- **Fonts** (`fonts.css`): `--wc-font-sans` = **Figtree** (body, loaded from Google Fonts) · `--wc-font-display` = **Schibsted Grotesk → Figtree** (headings; both OFL, from Google Fonts — Schibsted Grotesk is the free stand-in for Bloomwell's commercial *Gellix*) · `--wc-font-accent` = **Batangas → Figtree** (accents; Batangas is commercial, stack-loaded) · `--wc-font-data` = system **monospace** (verified data only — COA values, batch numbers, prices, IDs). Utilities: `font-sans` / `font-display` / `font-accent` / `font-mono`. Eyebrows are Figtree `font-semibold uppercase tracking-[0.16em]`, not mono. (Was Inter + TT Hoves through Aug 2026; swapped to Figtree + Schibsted Grotesk to echo the Bloomwell reference type pairing without licensing Gellix.)
- **Logo**: `src/components/brand/Logo.tsx` — `<Logo>` (full lockup image, size by height) and `<LogoMark>` (square mark image); `inverse` prop switches to the white artwork on dark surfaces. Uses the official PNGs in `src/assets/logos/` (`wecare-lockup-*` / `wecare-mark-*`, trimmed from `black ver.png` / `White ver.png`).
- **Liquid-glass + blue-gradient design language** (the "We Care" refinement, on top of the brand palette):
  - `<body>` paints a soft `--page-gradient` (sky → white → mint), fixed. `GradientBackdrop` (mounted in `RootLayout`) adds blurred colour orbs behind everything.
  - `.glass` / `.glass-strong` / `.glass-hover` (in `@layer components`, index.css) = frosted translucent surface + backdrop-blur + luminous border + soft shadow. Use on calm/marketing surfaces (cards, nav, panels); **keep forms, the COA table and the assessment options solid** for legibility. `.glass-strong` (higher opacity) for data panels (cart, checkout summary, question card).
  - `.image-glow` = radial "held" glow behind a floating product/hero image; pair with a `drop-shadow` on the `<img>`.
  - `--radius` = 20px; buttons are full pills; the `cta` button variant is a soft Azure→cyan gradient (`--cta-gradient`) with glow; `outline` buttons are frosted.
  - `sky-*` = the soft blue companion (gradients/orbs only — Azure stays the interactive colour). Shadow tokens: `--shadow-soft/float/glow`.
  - `AssessmentRing` arc = a blue→azure `<linearGradient>` stroke with a drop-shadow glow.
  - `SiteFooter` background is `--footer-gradient` (deep navy-blue → teal); its icon strip sits in a dark glass panel.
- Motion stays quiet (fade+rise reveal, 250ms glass hover-lift) except the Assessment Ring arc-fill.
- **Entrance motion (owner request, Aug 2026)** — two layers, both no-op under `prefers-reduced-motion`:
  - **Route-in:** `PageReveal` (`src/components/marketing/PageReveal.tsx`) wraps `<Outlet/>` in `RootLayout`; keyed by `location.pathname` so it replays on every navigation (a same-page hash nav like `/#how-it-works` does **not** re-trigger it). Fade + 12px rise, ~450ms.
  - **Scroll-in:** `Reveal` (fade + 10px rise via IntersectionObserver, one-shot). `Section` now self-reveals its content by default (`reveal` prop, default `true`) — future `<Section>` gets it free. The homepage sections + condition landing pages already choreograph their own children with `Reveal` (heading, then staggered card grids), so **they pass `reveal={false}`** to avoid a doubled rise. The other content pages — `LegalPage`, `FaqPage`, `CostsPage`, assessment `ResultPage` — now wrap their top-level blocks in `Reveal` directly. Dashboard / shop / cart / checkout / login stay static (short, form-heavy, some behind auth).
- **Dark appearance (owner request, Aug 2026)** — a real, wired dark theme, **not** a scaffold. Light is the default and the reference design; dark is a re-skin, **colour tokens only** (layout / type / motion shared). Mechanism: `.dark` class on `<html>`, toggled by `src/theme/` (`theme.ts` + `useTheme.ts`, a `useSyncExternalStore` store, no provider), persisted to `localStorage:wecare.theme`, applied pre-paint by an inline script in `index.html`. `ThemeToggle` (`src/components/layout/`) is a Light/Dark segmented switch next to `LanguageToggle` in the **footer only** (removed from the header, owner decision Aug 2026); strings in `common:theme.*`. **How the sweep works:** the `.dark` block re-defines the flat primitives (`--color-ink/-ink-muted/-surface/-surface-raised/-border`) *and* the Azure mid stops (`--color-petrol-600/700/800` → lighter, since on dark they're used almost only as accent text) *and* the pale `--color-sage-50/100` (→ dim panels) — so most `text-ink` / `bg-surface-raised` / `text-petrol-700` / `bg-sage-100` usages flip with **no per-component churn**. The strong solid `bg-petrol-600` active states were moved to `bg-primary` so that remap is safe. `Logo` auto-uses the white artwork in dark. `SectionHeading invert` intro / `FinalCta` subtitle / condition-hero subtitle use `text-white/80` (not `text-sage-100`, which is dark in dark mode); photos get a `brightness(.9) contrast(1.03)` trim (`.image-glow img` / `.hero-photo-mask` / `.image-fade-*` in `index.css`).
  - **Ground pass (Aug 31 2026, client feedback)** — tried and reverted: deriving the dark ground from light `--color-surface`'s own hue (`#f4f5fa` is HSL 230°/37.5%/97%, actually blue-violet, not teal) produced a ground that clashed against `--brand-band-gradient` / `--footer-gradient` / the sage panels — those are built around the brand's Azure/Dark-Azure hue (~191°) and don't move, so a differently-hued plain background read as "two colour schemes stitched together" (most visible where `Section tone="brand"` sits directly under the page background). Reverted to the brand-teal hue (matches every other dark-mode surface, so nothing else needed to change) and, per the follow-up ask, pushed it **much darker instead — "very dark teal, close to black."** Same hue/saturation as the original hand-picked ground (190.8°, 66%), lightness dropped from 11.6%/15.5% to **5%/8%**: **ground `#041215`, raised `#071d22`** (was `#0a2a31` / `#0e3a41`). Re-verified: body text 16.9:1 / 8.4:1, `--color-border` (`#518991`, unchanged) 4.4–4.8:1, Azure accent 7.2:1+ — all comfortably clear their minimums, and the brand bands now read as *more* distinct against the darker ground (luminance ratio band-vs-ground 2.3:1, was 1.2–1.8:1) instead of less. `--page-gradient`, `--input-background`, `--glass-bg/-strong` scale down with it (same hue family, same relative position between surface/raised as before).
  - **Accent-depth pass (Aug 31 2026, client feedback — "does the accent match?")** — asked right after the ground pass above, since the same accent colour (`--color-petrol-600/700/800`, used for headings/eyebrows/links) now sat on a much darker page. Checked hue first: every accent/ground/band token is within ~6° of each other (183–193°), so there was no hue clash. The real effect was contrast: those stops were tuned for the *old*, lighter ground, so unchanged lightness against the new near-black ground read far brighter than intended (`petrol-700`'s contrast rose from 6.87:1 to 9.67:1 with no colour change) — a pale, near-neon cyan against a much moodier page. Fixed by lowering lightness only (same hue/sat): 52.2%→44%, 68.6%→48%, 81.4%→58% → **`petrol-600` `#43949d`, `petrol-700` `#43a7b1`, `petrol-800` `#61bec7`** (was `#54acb6`/`#8bccd3`/`#b9e2e6`). Re-verified: 5.4/4.9:1, 6.7/6.1:1, 8.8/8.0:1 against surface/raised — all still comfortably clear of the 4.5:1 text minimum, now reading as a richer, more grounded teal instead of pastel cyan. `--color-petrol-400` (used separately for `--primary`/`--ring`/CTA-adjacent elements) was left alone — not the token flagged.
  - Residual, still-known-low-priority: native unchecked radios render slightly rusty under `color-scheme: dark`; the ring's `startLabel` pill + a few `[background-image:var(--cta-gradient)] text-white` decorative medallions keep near-white/white on teal; `tone="raised"` bands are very faint in dark; the condition-hero gradient stays literal (reads fine).
  - **Contrast QA pass (Aug 31 2026, client feedback — "verify dark mode with a QA tool")**: computed WCAG contrast ratios for every dark-mode text/background and border/background pair actually in use (this predates and motivated the ground pass above). One real failure found: `--color-border` (was `#21525b`) measured **1.4:1** against `--surface-raised` — under WCAG 1.4.11's 3:1 non-text-contrast minimum, and since `border-input` maps to this same token, **form-field outlines (login, checkout) were effectively invisible in dark mode**. Fixed by brightening `--color-border` to `#518991` — a semantic-token fix, so it flows through `border-border`/`border-input` everywhere with no per-component change; still valid (4.3–4.7:1) after the ground pass. The separate `dark:border-white/10` / `dark:divide-white/10` utility (glass-panel hairlines — `tone="raised"` bands, comparison-table dividers, cart/tile card edges, the header, the footer's always-dark `border-white/12`/`border-white/10`) measured worse still, ~1.35:1 — decorative (panels stay identifiable by background/blur without it, unlike a required form-field boundary), so literal 3:1 wasn't treated as a hard requirement; hitting it exactly needs ~white/38, which reads as a solid line and fights the liquid-glass "barely-there border" language. Brightened all 11 occurrences (owner request, Aug 31 2026) to `white/20` (footer's `/12` → `/20` too) — roughly +40% relative contrast, a meaningfully more visible hairline while staying subtle.
- `AssessmentRing` tones: `brand` (default) · `mint` · `deep`. `Section` tones: `surface` (transparent) · `raised` (faint frosted band) · `brand` (blue-teal gradient) · `mint` (mint wash).

### i18n — `src/i18n/`
- `config.ts` inits react-i18next: `de` default + fallback, `en` toggle. Namespaces: `common` (nav/footer/shared), `home`, `conditions`, `assessment`, `dashboard`, `shop`, `faq`, `costs`, `legal`. Add a new namespace rather than growing one. Language persists to `localStorage` (`wecare.language`) and syncs `<html lang>`.
- `useLanguage()` hook — `{ language, setLanguage, toggle }`.
- Locale JSON: `src/i18n/locales/{de,en}/<ns>.json`. DE and EN key parity is required — components reference keys dynamically.
- Interpolate variables inside strings (`"{{answered}} von {{total}}"`), never concatenate around `t()`.

### Client state — `src/features/*` (contexts mounted by `RootLayout` via `src/app/Providers.tsx`)
- `assessment/AssessmentContext` — answers + computed `result`, persisted to `localStorage` (`wecare.assessment`). Six questions in `questions.ts` (verbatim per "Short Assessment Flow"), logic in `recommendation.ts`.
- `cart/CartContext` — line items; `productId` is a **`SolutionId`**, `quantity` is always **grams**. `localStorage` (`wecare.cart`).
- `auth/AuthContext` — **MOCK** login, any email, `localStorage` (`wecare.auth`). No backend.
- `orders/orders.ts` — plain `localStorage` (`wecare.orders`) helpers (`OrderLine.productId: SolutionId`), written at checkout.
- `followup/followup.ts` — the Step-6 experience check-in ("How was your experience…?" → Good / stronger / lighter / another format / I need support), stored at `localStorage` (`wecare.followup`). Rendered by `DashboardFollowUpPage`.

### Products & shop — two layers
- **Solution layer (what the user sees)** — `src/data/solutions.ts`: the **5 named Solutions** — `night-now`, `calm-night`, `deep-ease`, `synergy-forte`, `synergy-ultra` — abstract wellness names, `tier` (`lighter`/`stronger`), `thcRange`, `priceEur` (€/g), `conditionKeys` (Deep Ease = pain only), `heroStrainId`, `strainIds[]`. Per-Solution **`category`** + **`blurb`** (the "Product Role Mapping" guideline: Sleep Support / Advanced Sleep Support / Body Comfort Support / Daily Balance Support / Advanced Balance Support + one-line role description) live in `shop:solutions.<id>.{category,blurb}` and render as a pill + line on every Solution card / result card / product header / landing preview. Helpers `SOLUTION_BY_ID` / `isSolutionId` / `solutionImage` / `solutionExampleCoa` / `solutionStrains` / `solutionsForCondition`. This is the id used by the cart, orders, the recommendation, and the `/shop/:productId` route.
- **Formats deliberately NOT built** — CBD Flowers (strain layer, post-assessment only), CBD Hash, Vapes, Aroma Pebbles are all "not homepage / not advertising / not problem-first" per "Product Role Mapping": no products, no routes, no marketing surface for them. Only add behind an explicit compliance decision.
- **Strain layer (fulfilment)** — `src/data/products.ts`: **19 real products** from the photos in `src/assets/products/` (18 flowers + Curaleaf inhaler). Each Solution is dispensed as one of its `strainIds` after the prescription; the product page shows them under "Dispensed as". Genetics, prices, origin, COA, irradiation are **placeholders**.
- `recommendation.ts` — `getRecommendation(answers)` → `{ problem, primarySolutionId, secondarySolutionId, secondaryIsAdvanced, gentleFirst, requiresMedicalReview, explanationKey }`. The `PAIR` table fixes primary + secondary per problem ("Recommendation Logic" guideline): sleep → Night Now / Calm Night · pain → Deep Ease / Synergy Ultra · stress → Synergy Forte / Synergy Ultra · migraine → Synergy Forte / Deep Ease. **The primary never changes** — a new user (q5 = "No, I am new") is never led with the stronger option. Strong/very-strong severity or prior cannabis/CBD use only sets `secondaryIsAdvanced` (Result page then labels the secondary "Advanced option"). `gentleFirst` (new / mild / moderate) shows the "start gentle, oil-first" nudge. `requiresMedicalReview` is **always true** → Result always routes through Medical Review. `pairCounterpart` / `matchedSolutionIds` are the external helpers (follow-up, landing pages).
- Shop pages (`ShopIndexPage` = 5 Solution cards, no filters; `ProductPage` = Solution detail with gram selector, "Dispensed as" strain list + example COA) . **Kept out of primary nav** — reachable only post-assessment, from the footer, and the dashboard. Strain photos never appear on homepage / landing pages (landing pages show a low-emphasis "What you might be matched with" Solution preview only).

### Components
- `src/app/components/ui/*` — shadcn/ui (Radix), **trimmed to what's in use**: `button`, `input`, `label`, `sheet`, `accordion`, plus `utils` (`cn`). The other ~40 vendored components + their exclusive deps (MUI, react-dnd, recharts, react-slick, embla, vaul, cmdk, sonner, most `@radix-ui/*`, …) were removed as Figma-Make-demo remnants. Re-add a shadcn component with `npx shadcn@latest add <name>` if you genuinely need one.
- `src/components/brand/AssessmentRing.tsx` — the one signature visual device (progress arc / hero decoration / result reveal / dashboard indicator). Never a spinner. Respects reduced-motion.
- `src/app/components/figma/ImageWithFallback.tsx` — `<img>` with inline-SVG fallback; keep.
- The old hand-rolled `atoms/molecules/organisms/shared` set has been **removed** — build with the shadcn primitives above (add more via the CLI if needed).

### Entry
`src/main.tsx` (StrictMode, imports `./i18n/config` for side effects) → `src/app/App.tsx`. `App.tsx` is currently a **temporary foundation preview** (palette + type + ring), replaced from the navigation/homepage phase onward.

### Assets  (each folder has a README)
- `src/assets/logos/` — official WeCare logo (`wecare-lockup-*` / `wecare-mark-*`), used by `src/components/brand/Logo.tsx`.
- `src/assets/icons/` — third-party **payment + DHL shipping** marks → `src/components/layout/FooterIcons.tsx` footer strip (`TrustBadges` only). The social links (`SocialLinks` / `SOCIALS`) and the Google Play / App Store badges (`AppBadges`) were removed — owner decision Aug 2026: they only pointed at `#` and there's no native app. The now-unimported image files (social logos, store badges) are still in the folder.
- `src/assets/products/` — the 19 real product photos → `src/data/products.ts` via `src/data/productImages.ts` (glob, matched by filename).
- `src/assets/images/` — marketing photography → `src/data/siteImages.ts` (glob, now `.png`/`.jpg`/`.jpeg`, `IMG` map). Wired: homepage hero (`IMG.homeHero` = `Knowledge Hub/Hero`, the landscape photo) + 4 problem-card banners + "Simple recommendations" right-hand photo (`IMG.homeSolutions` = `hero section.png`, the portrait phone cut-out) + final-CTA photo (`IMG.homeDoctor` = `assessment-2.png`) + "How WeCare works" step cards (`IMG.process` — step 2 `assessment.jpg`, step 3 `results.jpg`, steps 1/4 `Knowledge Hub/60`/`64`); the 4 condition landing-page heroes; the login page's left welcome panel (`IMG.login` = `Homepage/13`, `lg`+ only). **Aug 2026 the three homepage photos were rotated one section along** (owner request), then hero and "Simple recommendations" were **swapped back** (owner call — the tablet/mug landscape photo reads as self-care better than the phone cut-out for a rotating-problem hero); **the final-CTA and step-2/3 photos were swapped again** to `assessment.jpg`/`assessment-2.png`/`results.jpg` (owner request) — `Homepage/Hero.png` (the former final-CTA telehealth cut-out; still used as the General Wellness fallback hero via `IMG.conditionHero.generalWellness`) and `Homepage/8.png`/`Knowledge Hub/61.png` (the former step 3/2 photos) are now unreferenced by those slots. `How it works Page.png` and `Condition Page.png` remain **unreferenced**. Per-condition section photos are available but not yet placed. The "Knowledge Hub" folder name is a leftover from the removed Knowledge Hub page — the photos in it are just generic marketing stock now, several already wired elsewhere.

### Homepage & landing structure (guideline-locked)
- **Homepage** (`src/pages/HomePage.tsx` → `home/sections.tsx`, `home` ns) — the sections, in the **current render order**: (1) Hero — headline + subheadline, Primary CTA "Start Free Assessment" / Secondary "How It Works", **5** trust points. The headline is `hero.titlePrefix` ("Find the right support for") on line 1 + a **`<RotatingWord>`** (`src/components/marketing/RotatingWord.tsx`) on line 2 — **larger (`text-[1.4em]`) + bold, in Azure** — that cycles the 4 `hero.problems` (Sleep / Pain / Stress & Anxiety / Migraine) with a quiet crossfade + rise (~400ms, every 2.8s, first swap after ~1s). It's `aria-hidden`; an `sr-only` `hero.title` (the full static sentence) is the screen-reader + `prefers-reduced-motion` text (reduced motion renders the words joined, no cycling). Own-line so the varying word width never shifts layout. Owner request Aug 2026 (inspired by DoktorABC's rotating hero word). (2) "What do you need help with?" — 4 problem cards; (3) "Simple recommendations. No confusing catalog." — **centered** section heading, then a 2-col split: 4 support cards (Sleep / Body Comfort / Daily Balance / Head Tension Support, **no prices**) on the left, a single anchor photo (`IMG.homeSolutions`, `lg`-only) on the right. (Was: heading in the left column above the cards, and the right column a per-problem `ComboCarousel` of solution pairs + a "Start Assessment" CTA — carousel + CTA removed, heading centred, owner decision Aug 2026.); (4) "A guided and responsible experience" — heading + **6** badges, **no photo** (the photo that was here moved to section 3, owner decision Aug 2026) (spec said 5; a "No obligation" badge was added later, owner request — fills the 3+3 grid); (5) **"How WeCare works"** — **4** steps (choose / assessment / match / continue); (5.5) **`DeliveryBannerSection`** — a slim `--cta-gradient` banner ("Fast delivery, all across Austria." · city list · "order before 12:00 for next-day delivery") sitting between How-It-Works and the final CTA, client feedback Aug 31 2026 ("so customers see WeCare's reach" ahead of the big ask) — deliberately problem-first-safe copy (no "cannabis"/"treatment" as the hook, unlike the stakeholder's original draft) and no map graphic (no such asset exists; text + `MapPin` icon carries the message); (6) final CTA; (7) `ComparisonSection` ("Two ways to find what helps", guided vs. catalog — owner addition; moved to sit *after* the final CTA, owner decision Aug 2026 — a reinforcing "why WeCare" beat between the CTA and the FAQ, keep it there); (8) **`FaqSection`** — a curated **5**-question accordion (`home:faq.*`), links out to the full `/faq` page (owner addition, Aug 2026). `HowItWorksSection` was briefly rendered *after* the final CTA (Aug 2026) and then moved back to just *before* it (owner decision) when the FAQ section was added; it still carries `id="how-it-works"` + `scroll-mt-24` as the redirect target for the retired `/how-it-works` page, and `ScrollToHash` finds it wherever it sits. A visual-enrichment pass (Aug 2026) added: the layered hero (photo + `AssessmentRing` decoration arc + two info chips that **rise in on load** (each wrapped in a staggered `<Reveal delayMs={450/650}>` on the outer `absolute` wrapper) and then **gently bob** (`.wc-float-a` / `.wc-float-b` in `index.css` — the bob stays on the chip itself so it doesn't fight the Reveal's `transform`; owner request Aug 2026, *reversing* the earlier "chips must be static" decision; the **photo and ring still do not drift**), plus the ring's arc sweeping in once on load (`animate drawDurationMs={2400} drawDelayMs={250}` — slowed from 1400 on owner request Aug 2026; `AssessmentRing`'s own sanctioned, reduced-motion-aware animation), with the trail dots + "7/10" pill fading in as it lands), a photo + vertical step-timeline in a `glass-strong` panel for How-It-Works, Azure icon medallions on the section-3/4 cards + a right-hand anchor photo on section 3 (section 4's photo was later removed), and a bottom-faded (`.image-fade-b`) portrait on the final CTA. Below-fold images carry `width`/`height` + `loading="lazy"`. (A testimonials section was removed earlier — fabricated quotes/names, never part of the spec.)
- **Problem landing pages** — `ConditionLandingPage.tsx`, one template, `conditions` ns. URLs are now bare slugs: `/sleep-problems` · `/pain-body-discomfort` · `/stress-anxiety` · `/migraine-head-tension` (old `/conditions/*` paths 301-redirect). Per page: hero (headline/subheadline + a **single** CTA — the assessment; the secondary "How it works" outline button was dropped, owner decision Aug 2026; the CTA is `w-full sm:w-auto`) → "What this is about" (explanation paragraph **followed by** the "Common situations" 5–6-bullet checklist as an `<h3>` sub-block in the same `surface` section — owner merge, Aug 2026; no longer its own section. Section tone was `raised` briefly, then set to `surface` — owner decision Aug 2026 — so it shares the transparent page-gradient background with the "What you might be matched with" section below, rather than reading as a separate frosted band) → "How WeCare helps" (4 per-page steps, `<key>.helpSteps`) → **the "What you might be matched with" 2-panel `surface` section** (owner layout, Aug 2026): left column = the section heading + intro (`shared.matchedHeading` / `shared.matchedNote`) then **`<MedicalNotice>`** (side-effects + "not a substitute for standard therapy" + "not individual medical advice"; `common:medicalNotice.*`); right column = **`<ComboCard problem={key} showHeader={false}>`** (`src/components/marketing/ComboCard.tsx` — the matched pair as **name + category + one-line blurb icon-medallion tiles + combined THC line, NO product/strain photos**; `ComboCarousel` was deleted when the homepage carousel was dropped, so `ComboCard`'s `showHeader` path now has no caller) then the assessment CTA (`w-full lg:w-auto` — full-width while the two panels are stacked). On mobile the whole 2-panel row is `flex flex-col` ordered heading → matched pair + CTA → medical notice (`lg` reverts to the 2-column grid via auto-placement + `lg:row-span-2` on the pair). `general-wellness` skips this section's grid and shows `<MedicalNotice>` centred on its own. **The old bottom `tone="brand"` CTA section was removed (owner decision, Aug 2026)** — the hero CTA + this section's CTA are enough; `shared.finalCtaHeading` / `shared.notSurePrompt` are now unused, and the only in-page link to `/general-wellness` is on the 404. `general-wellness` is a hidden fallback, never counted as one of the 4.
- `public/` — `favicon.png` (512², white mark on Azure tile) + `apple-touch-icon.png`, linked from `index.html`.

Every glob resolver renders through `ImageWithFallback` (`src/app/components/figma/`), so a wrong key degrades to a placeholder instead of breaking.

### Footer
`SiteFooter` is **dark** (`bg-petrol-900`, uses `<Logo inverse />`). Below the link columns: a shipping + payment badge strip (`FooterIcons.tsx` `TrustBadges` — DHL + Visa/Amex/Mastercard/Google Pay/Apple Pay/Klarna, static images), then a bottom bar with `footer.copyright` + `footer.emergencyDisclaimer` ("WeCare does not replace emergency care") + `ThemeToggle` + `LanguageToggle`.

### Vite
`@` → `src/` alias. `figma:asset/<f>` imports resolve to `src/assets/<f>`. Keep the React + Tailwind plugins.

## Build order (spec Section 13)

1–10 ✅ complete: audit · design tokens/fonts/i18n · navigation · homepage · four problem landing pages (+ General Wellness demoted) · assessment engine + recommendation logic · result page · Recommended Solution page + COA section · shop/cart/checkout out of nav · DE/EN pass across everything touched. (`MedicalReviewPage` still exists at `/assessment/medical-review` but the Result CTA now goes straight to the product page per the consolidated doc.)

### Client-guideline pass — `../Downloads/WeCare Website Structure.md` is the consolidated source of truth; every section 1–17 is implemented and browser-verified, **except the owner overrides listed at the end of this file** (nav, homepage section order, hero motion)
- **§1–2 Main Direction / 4 Problems** — problem-first guided flow; exactly 4 problems; 5 named Solutions backed by the 19 strains.
- **§3/§9 Core Flow + Result Page** — assessment shows "~60–90 s · not a medical form" + a linear progress bar; Result headline "Your recommended solution is ready." / subheadline / "Selected problem·Frequency·Strength·Explanation" / CTAs "View Recommended Solution" (→ product page, not medical review) + "Change My Answers" / exact disclaimer. **Beginner-simplification pass (Aug 2026, owner/client-feedback)** — a stakeholder (via a beginner-perspective review) flagged the old side-by-side two-card layout as too technical (`THC 20–24 %` vs `27–33 %` shown bare, before any explanation) and a drop-off risk. Restructured: one dominant `PrimaryRecommendationCard` (name · category · the "why" explanation inline · THC range + price collapsed behind a `<Accordion>` "THC & ingredient details" disclosure · the CTA) with the secondary solution demoted to `AlternativeSolutionLink`, a single quiet link row ("Prefer something else? → [name] · Also available / Advanced option"), not a second competing spec card. Deliberately did **not** adopt the client's literal "Gentle option / Stronger option" badge wording — "Recommended for you" asks the user to trust the match rather than parse a strength axis, and a visible Gentle/Stronger pairing would re-introduce the side-by-side comparison framing the client also asked to avoid; the gentler-first behavior is already enforced by `recommendation.ts`'s `gentleFirst` rule and explained in prose to exactly the users who need it (the nudge box only shows when `gentleFirst` is true). As a middle ground, `AlternativeSolutionLink` takes an optional `hint` — a small `text-ink-muted/70` caption ("Usually a stronger option, for later.") shown only in the non-advanced case (where `label` doesn't already say "Advanced option"). `result.explanationKey` moved from its own section below both cards to inline in the primary card, ahead of any numbers. The bottom CTA row now holds only "Change My Answers" (the primary CTA lives on the card).
- **§4 Homepage / §5 Landing Pages / §6 Assessment / §7 Recommendation Logic / §8 Product Role Mapping** — see "Homepage & landing structure" and "Products & shop"; question labels + CTA + fixed primary/secondary + "Advanced option" labelling + per-Solution category/blurb all match verbatim.
- **§10 Recommended Solution Page** — name · category · why · who-it-suits · **product format** · **key ingredients** · COA (+ "Lab tested" badge) · FAQ · CTA (Add to cart / Check availability — no aggressive language). `JourneyStepper current="product"`.
- **§11 Checkout** — summary shows name·qty·price·**Delivery**·Total; form has customer details · shipping · **payment method** (invoice / bank transfer) · **Terms confirmation** + the required **"not intended to diagnose, treat, cure or prevent disease"** checkbox — Place order stays disabled until both are ticked. `DELIVERY_FEE_EUR = 0`.
- **§12 Dashboard** — `DashboardLayout` menu = Overview · My Assessment · My Recommendation · My Orders · Follow-up · Support · Profile. `DashboardHomePage` (index) shows last problem · recommended product · order status · follow-up reminder · support button. `DashboardAssessmentPage` moved to `/dashboard/assessment`.
- **§13 Follow-up** — `DashboardFollowUpPage`: prompt + 5 options → then 4 actions (reorder / try secondary / retake / contact support) + "Update My Recommendation" CTA + the 14–21-day window note. State in `followup/followup.ts` (`wecare.followup`).
- **§15 Legal & COA** — `/legal/{terms,privacy,cookie-policy,imprint,shipping-policy,refund-policy}` have real **draft** content (`src/pages/legal/LegalPage.tsx`, `legal` i18n namespace — see "Legal page content" below) + `/contact` (still a `PagePlaceholder`) + real **`/lab-tests`** (`LabTestsPage`) table: CBD·CBG·CBN·THC·batch·test date·safety per Solution. The footer's **Legal** column holds exactly those 6 `/legal/*` documents (owner decision, Aug 2026); `/contact` and `/lab-tests` are footer-linked from the **WeCare** column instead, since they aren't legal documents. **Product Disclaimer** (`/legal/product-disclaimer`) was dropped (owner decision, Aug 2026) — the required "not intended to diagnose, treat, cure or prevent disease" language still ships as the checkout confirmation checkbox (`shop:*.disclaimerLabel`), so the compliance requirement is met without the standalone page. A `/dashboard` ("My area") footer link was tried and dropped too — with mock auth and no persistent session it just redirected to Login for everyone.
- **§16 Design** — progress bar + trust badges + lab-test badges added; no cannabis-leaf / smoke / recreational visuals (brand hard rule).
- One standing tension: "Short Assessment Flow" / "Final MVP" say oils are the main solutions, but the 5 Solutions are format-agnostic — resolved as *sequencing* (new users never led with the stronger Solution) + *copy* (gentle nudge + the product-page "Product format" line frame oil as the controlled starting format), not a separate oil SKU. CBD Flowers/Hash/Vapes/Aroma Pebbles remain unbuilt per §8.

**Content still stubbed**: `/contact` is a localized `PagePlaceholder` (title + one-line description only). `/about`, `/careers`, `/providers` were removed (owner decision, Aug 2026) — pure marketing filler with no real content and no footer link; do not re-add. `/contact` stays a stub — it's a real link target from the FAQ page and Dashboard Support page, but nobody's asked for its content yet. COA values, product data, auth backend, per-product FAQ are placeholders.

**Legal page content** (Aug 2026; rewritten from owner-supplied source Aug 31 2026) — the 6 `/legal/*` documents have full **draft** content, not `PagePlaceholder` stubs: `src/pages/legal/LegalPage.tsx` renders sectioned content (`heading` + `body`/`list`/`fields`/`bodyAfter`) from the `legal` i18n namespace (`legal:docs.<doc>.sections`), with a TOC once a doc has >4 sections. **All six docs were re-authored from source files the owner supplied (`../Downloads/WeCare — <Doc>.md`), Aug 31 2026**, replacing the earlier from-scratch drafts: **Terms** transcribed near-verbatim from a complete 22-section source (+ an unnumbered "Acceptance" lead-in = 23 sections) and carries a **real effective date, 31 August 2026**; **Cookie Policy** (9 sections) and **Impressum** (10; drops the discontinued EU-ODR link) transcribed from the source files' policy bodies (reviewer commentary in those files was skipped); **Privacy Policy** (26 sections) was *drafted by Claude from the source file's structural outline* — the source had no prose — covering GDPR Art. 6/9 bases, a dedicated health-data section, and the 3-layer wellness/assessment/medical model; **Shipping** (10) and **Refund** (7) transcribed from their source bodies. DE is a full parallel translation (formal *Sie*), EN/DE section-`id` parity enforced. **Gendering style (client feedback, Aug 31 2026)**: formal DE copy uses the slash form — `Nutzer/innen`, `Kund/innen`, `Verbraucher/innen` — never the gender-colon (`Nutzer:innen`); a global pass fixed 29 instances across `legal.json`, `home.json` and `shop.json`. Keep this in mind for new DE strings. **"Assessment" in formal DE legal text (client feedback, Aug 31 2026)**: a stakeholder flagged the English loanword "Assessment" as wrong-register for stiff formal AGB German. Scoped the fix to `legal.json` **only** — 34 sentences across Terms/Privacy/Cookies/Impressum reworded to "Fragebogen" (masculine — `der/den/des Fragebogen(s)`, with matching relative pronouns and adjective endings, not a blind token swap), including the Terms glossary entry itself (`Fragebogen — der Online-Fragebogen, mit dem...`). The **product UI keeps "Assessment" verbatim everywhere else** (`home`/`assessment`/`common`/`dashboard` namespaces) — it's the spec-defined term used throughout DE marketing/product copy ("Kurzes, geführtes Assessment" etc.) and the stakeholder's note read as being about legal-register only, not a site-wide rename. Austria language rules throughout (never "treats/cures", "recommended solution" not "prescription", "ärztliche Prüfung"). The visible `legal:draftNotice` banner was removed (owner request, Aug 31 2026) and every entity placeholder was filled with an obviously-provisional value (`WeCare GmbH`, `Musterstraße 1, 1010 Wien`, `FN 000000a`, `ATU00000000`, `Max Mustermann`, `*@wecare.example`, effective date `31 August 2026`) — swap for the real registered details before launch. **This is drafted/transcribed text, not reviewed legal advice** — have Austrian/EU counsel review before launch (especially the Privacy Policy: Art. 9 basis + controller/processor structure for the doctor and pharmacy). Shipping's "Delivery times" section (Aug 31 2026, client-feedback pass) states the real courier policy — orders **ready to ship** by 12:00 typically arrive the next day, after 12:00 within two days, across all of Austria — deliberately scoped to *dispatch*, not order placement, so it doesn't contradict the separate 24–48h medical-review turnaround.

**Knowledge Hub removed** (Aug 2026, owner decision) — the index page, article template, `src/data/knowledge.ts`, the `knowledge` i18n namespace, and every link/button pointing at `/knowledge-hub*` (footer, router) are gone. It previously shipped with every article body showing an "in preparation" placeholder instead of real content; rather than leave that live, the owner had it pulled entirely. Don't re-add a `/knowledge-hub` route without new real content behind it. The image files under `src/assets/images/Knowledge Hub/` are untouched — several are wired into other pages (see the Assets section).

**Owner overrides / additions vs the spec (Aug 2026) — keep these, don't "fix" them back:**
- Primary nav trimmed to the 4 problems (no How It Works / FAQ).
- "How WeCare works" homepage section sits just before the final CTA; `/how-it-works` redirects to it. A curated homepage FAQ section (`FaqSection`, 5 Q&As, `home:faq.*`) sits after the final CTA and links to the full `/faq` page.
- Homepage has an added `ComparisonSection` (guided vs. catalog). It now renders **after** the final CTA (owner decision, Aug 2026), between the CTA and `FaqSection` — was briefly between trust and the final CTA. `ComparisonSection` **and** `FaqSection` are `tone="surface"` (were `tone="raised"` frosted bands with a `border-b-0`/`border-t-0` merge) — owner decision Aug 2026: the page gradient must flow unbroken from the final-CTA band all the way to the FAQ, with no visible section edge.
- `/faq` is a real footer-linked page (`FaqPage.tsx`, `faq` ns), not in the primary nav.
- `/costs` — qualitative "what to expect / what it costs" page (`CostsPage.tsx`, `costs` ns), **no euro figures** (exact fees depend on the medical review); footer-linked; checkout summary links to it via a review-fee note.
- `<MedicalNotice>` (`common:medicalNotice.*`) on every condition landing page.
- Knowledge Hub removed (see above) — was built (index + topic filter + `/knowledge-hub/:slug` template) but every article body was an "in preparation" placeholder; pulled rather than shipped that way.
- Hero **photo** is static (no float). The hero **info chips** rise in on load (staggered `Reveal`) and then gently bob (`.wc-float-a/-b`, owner request Aug 2026 — reverses the earlier static-chips decision). The ring's arc sweeps in once on load, `drawDurationMs={2400}` (slowed from 1400, owner request Aug 2026). All reduced-motion-aware.
- `Solution.oilFormulation` (`src/data/solutions.ts`) — founder-spec CBD-oil profile, renders as an "Oil formulation — starting format" block on the Recommended Solution page, distinct from the flower COA.
- **No product / strain photos before the assessment (owner + compliance, Aug 2026)** — a stakeholder flagged the bud photos in the pre-assessment "matched pair" preview as a legal risk ("grass pics only after the questionnaire"). `ComboCard` was reworked to show **name + category + one-line blurb icon-medallion tiles + combined THC line only — no `solutionImage` / `ImageWithFallback`**. Then the homepage carousel itself was dropped (owner decision — see homepage §3): `ComboCarousel.tsx` deleted, the section's right column is now just a photo, and `home:solutionsPreview.cta` / `combo.{prev,next,startHere}` keys were removed. `ComboCard` survives only on the condition-page "What you might be matched with" section. Product/strain imagery stays post-assessment (Result page `RecommendationCard`, `/shop/*`). This finally makes the long-standing hard rule ("strain photos never on homepage / landing pages") actually true in code.
- **"We care" tagline voice (owner request, Aug 2026)** — the brand name is played on as "we care" in a few EN headline/tagline spots: `home:hero.kicker` ("Because we care"), `home:finalCta.title`, `common:brand.tagline` (footer), `assessment:result.reassure` (line under the result headline). (`conditions:shared.finalCtaHeading` was added for the condition pages' bottom CTA `<h2>` but that whole section has since been removed — the key is unused.) The pun is **EN-only** — DE keeps the same warmth without forcing the name (e.g. "Weil du uns wichtig bist" / "Uns ist wichtig, wie es dir geht"). Button labels stay literal; don't spread the pun onto CTAs or body copy.
- Repo is under git — feature work goes on a branch off `master`.

## Product-owner audit fix pass (2026-09-01, branch `audit-fixes`)

A full live-browser PO/QA acceptance audit (see `../AppData/.../scratchpad/wecare-audit.html`) produced 25 findings + legal items. The code-fixable ones were applied on `audit-fixes`; `pnpm typecheck` + `pnpm build` green, DE/EN key parity re-verified across all 9 namespaces.

- **WC-01 (P1) — checkout confirmation.** "Place order" landed on the empty cart, not `/shop/confirmation`. Cause: `CheckoutPage`'s `items.length === 0` guard re-rendered after `clear()` and pre-empted the navigation. Fix: a `placed` flag exempts the guard during submission; `OrderConfirmationPage` also falls back to `getOrders()[0]` when `location.state.orderId` is absent (refresh / direct visit). Confirmation screen now always shows.
- **WC-02 (P1) — analytics seam.** `src/lib/analytics.ts` — a `track(event, props)` helper gated on consent (`analyticsAllowed()`), dev-console in dev, an empty `dispatch()` to wire a vendor into. Instrumented at the funnel points: assessment start / per-question / complete, recommendation view + CTA, product view, add-to-cart, checkout start, order placed, login/logout, follow-up. **No vendor is wired** — that's still a launch task.
- **WC-03 (P2) — real `/contact`.** `src/pages/content.tsx` `ContactPage` replaced the `PagePlaceholder`: support email (from `src/config.ts` `SUPPORT_EMAIL`, placeholder `support@wecare.example`), support hours, a `mailto:`-composing form, emergency-number notice. `PagePlaceholder.tsx` is now unreferenced.
- **WC-04 (P2) — deep-link problem for returning users.** `AssessmentContext.prefillProblem` now applies `?problem=` even over a different saved `q1` (and clears `completedAt`); it only no-ops when the linked problem already matches. Uses an `answersRef` to avoid a stale closure.
- **WC-05 (P2) — assessment format jargon.** Added `questions.q4/q5/q6.hints.*` (+ `q6.note`) in DE & EN; `AssessmentEnginePage` renders a muted sub-line per option when a hint exists, and a note under the legend. Q2/Q6 still don't feed `getRecommendation` (WC-15) — an owner decision, left as-is and noted here: **Q2 (frequency) and Q6 (format preference) are captured for the medical reviewer / future personalisation but do not alter the deterministic recommendation.**
- **WC-06 (P2) — checkout terms links.** `shop:checkout.termsLabel` now carries `<terms>`/`<privacy>` tags rendered via `<Trans>` to `Link`s to `/legal/terms` and `/legal/privacy` (`termsLabelPlain` kept as a fallback string).
- **WC-07 (P2) — consent.** `src/features/consent/` (`consent.ts` store + `useConsent.ts`, same `useSyncExternalStore` pattern as `theme/`), `ConsentBanner` mounted in `RootLayout`, "Cookie settings" trigger in the footer bottom-bar (`reopen()`). Choice persists to `localStorage:wecare.consent` (`unset`/`essential`/`all`); analytics gates on `all`. **A real CMP + self-hosted fonts are still launch tasks.**
- **WC-08 (P2) — editable profile.** `AuthUser` gained `phone?`; `AuthContext.updateProfile` patches name/phone and persists to `wecare.auth`. `DashboardProfilePage` has an edit/save/cancel form (email stays the identity, read-only).
- **WC-10 (P3) — dashboard recommendation.** `DashboardRecommendationPage` shows the solution **category** instead of a bare `THC {range}` side-by-side (mirrors the beginner-safe result page). `dashboard:recommendation.primary/secondary` reworded to "Recommended for you" / "Also available".
- **WC-11 (P3) — cart badge.** `CartContext` exposes `lineCount` (distinct products); the header badge uses it instead of total grams.
- **WC-12 (P3) — login.** Password field is now `required minLength={8}` (auth is still mock — the value is ignored).
- **WC-13 (P3) — per-session data.** `AuthContext` clears `wecare.assessment/cart/orders/followup` on sign-out and on a different-email sign-in (keeps `language`/`theme`/`consent`). `Providers` keys the Assessment + Cart providers by `sessionKey` so their in-memory state reloads. First-time login does **not** clear (the add-to-cart → sign-in → checkout path is preserved).
- **WC-14 (P3) — gentle nudge.** `assessment:result.gentleNudge` no longer opens "New to medical cannabis?" (it fires on mild/moderate severity regardless of experience).
- **WC-16 (P3) — homepage order / delivery dup.** The rendered order (final CTA last, after Comparison + FAQ) **is** the intended one — `HomePage.tsx` says so; this doc's earlier "final CTA before Comparison" description was stale and is corrected by this note. The `trust.badges` delivery line was trimmed so it no longer repeats the `DeliveryBannerSection`'s "order before 12:00" copy.
- **WC-18 (P4) — result disclosure.** The accordion now also shows a "Starting-oil profile" row (`CBD · CBG · CBN · + Melatonin` from `oilFormulation`); label renamed `detailsLabel` → "THC, price & ingredients" and the price row label to `solution.priceLabel`.
- **WC-20 (P4) — launch files.** `public/robots.txt` (Disallow-all, pre-launch) + `public/sitemap.xml` (public routes, `wecare.example` origin). `usePageTitle(title, description?)` now also syncs `<meta name="description">`; wired on home / conditions / assessment / result / faq / costs / lab-tests / legal.
- **WC-21 (P4) — nav breakpoint.** Desktop nav switched from `xl:` to `lg:` in `SiteHeader` (full nav from 1024px; hamburger below).
- **WC-22 (P4) — skip link.** `RootLayout` renders a visually-hidden "Skip to main content" link (`common:a11y.skipToContent`); `<main id="main-content">`.
- **WC-23 (P4) — landing-page THC.** `ComboCard` no longer renders the "THC across the pair · X–Y %" line in its `showHeader={false}` (landing-page) path — conservative reading of the problem-first "no product technicals before the assessment" rule. Flag for legal if they want it back (trivial to restore).
- **WC-24 (P4) — DE copy.** `result.explanations.sleep/stressAnxiety` "hochdosieren" → "falls du mehr brauchst"; `result.intro` "gematcht" → "ausgewählt".

### Second fix round (same day) — the remaining judgment-call items

- **Error boundary** (Technical-Audit gap, not a numbered WC). `src/app/RootErrorBoundary.tsx` (class component) wraps `<RouterProvider>` in `App.tsx` and shows a bilingual fallback (language from `localStorage`) with reload / home actions instead of a blank page. `componentDidCatch` logs — seam for a real reporter.
- **Self-hosted fonts** (WC-07 / L-4 sub-item). Figtree + Schibsted Grotesk (both variable, 4 woff2 total) vendored into `public/fonts/`; `@font-face` blocks in `src/styles/fonts.css`; the Google Fonts `<link>` + `preconnect`s removed from `index.html` (replaced with `preload` for the two latin woff2). **The app now makes zero request to `fonts.gstatic.com`** — verified in the browser. Re-download from the css2 endpoint on a version bump.
- **WC-09 — orphaned `MedicalReviewPage` removed.** Deleted `src/pages/assessment/MedicalReviewPage.tsx`, its route + import in `router.tsx`, the `AssessmentStartPage`-barrel re-export, `paths.assessment.medicalReview`, and the `medicalReview` i18n blocks (`common` + `assessment`, DE & EN). `/assessment/medical-review` now 404s. The scaffold is in git history for whoever builds the real medical-review flow. (`recommendation.ts` `requiresMedicalReview` and the legal-doc prose about "medical-review process" are unrelated — kept.)
- **WC-19 — final-CTA image.** `IMG.homeDoctor: "assessment-2"` → `"Homepage/Hero"` (a person on a video call with a doctor — no readable UI text; `assessment-2.png` was a laptop mock-up of the **EN** assessment UI showing on the DE homepage). `assessment-2.png` is now unreferenced.
- **WC-25 — step-4 "How WeCare works" image.** `IMG.process.continue: "Knowledge Hub/64"` → `"Knowledge Hub/63"` (a courier handing over a parcel — `64.png` was a spread of prescription pill bottles, product-forward for a "what's next" step). `64.png` is now unreferenced.
- **"demo" copy softened.** `dashboard:auth.demoNote` and `shop:checkout.paymentNote` reworded to drop "demo" / "backend" / "credentials" — now say "this preview signs you in with any email address" and "no card details are collected here — you pay by invoice or bank transfer once a doctor has approved your prescription."

**Still NOT fixed (genuinely need backend / a vendor account / an image pipeline / counsel):** WC-02 analytics vendor wiring; WC-07 a real consent-management platform (the banner + gate are in place); WC-17 (WebP/AVIF + `srcset` — deliberately left; adding `sharp` + a Vite image plugin is a build-infra decision, not an audit-fix, and risks the team's build); every §21 legal item (real entity data, counsel review, real COA data, Austria advertising review); and the mobile device pass the audit could not run.

## Product-owner decisions applied (2026-09-01, branch `audit-fixes`)

The PO returned authoritative direction on the 26-item decision brief (`../AppData/.../scratchpad/wecare-owner-decisions.html`). The "implement now" set is done; the "needs real data" items are configurable + production-blocked, nothing invented.

- **D1 — Q2 / Q6 activated as personalisation, NOT medical logic.** `getRecommendation()` is unchanged (still reads q1/q3/q4/q5 only — no THC/strength change from "daily"/"very strong"). The Result page now adds: a `frequencyNote` (if q2 answered — "noted for your medical review"), and a `formatPreferenceNote` (only if q6 = flower/vape — acknowledges it, then redirects to oil-first / beginner-safe). Q5/Q6 already carry plain-language hint lines from the audit pass.
- **D2 — "Pain & Body Comfort".** `common:nav.pain` → "Pain & Body Comfort" / "Schmerzen & Körperkomfort"; the Pain landing hero now says "Living with daily pain or body discomfort?"; `conditions:pain.shortTitle` aligned. Body copy keeps the softer "discomfort" wording. (Nav label is longer now — flag for the D25 mobile pass.)
- **D3 — medical-review flow (UX / state model).** New `src/features/review/review.ts` (mock `wecare.review` store, 6-status enum `submitted·inReview·infoRequired·approved·notApproved·consultation`, holds the assessment answers "handed to the doctor"). New `ReviewStatusPage` at `/assessment/review` (`paths.assessment.review`) — recreates the page removed as WC-09, now connected to the status model with per-status copy + a 4-step explainer. Result-page primary CTA is now **"Submit my assessment for medical review"** → creates the review → `/assessment/review`; "view the recommended solution" demoted to a quiet link. Dashboard Overview + "My Recommendation" surface the real review status. `result.next.*` steps reworded to the submit → review → order-&-delivery order. Product page gains an "orderable once approved" line. Real doctor updates + emails are the backend (D24).
- **D6 — no fake prices as final.** `PRICES_CONFIRMED = false` in `src/config.ts`; while false, product page / cart / checkout show `shop:pricesIndicative` ("indicative — confirmed after your medical review"). Prices stay in `solutions.ts` for now but a real build serves them from pharmacy config (see `docs/BACKEND-ARCHITECTURE.md`).
- **D7 — payment badges.** The 6 card/wallet/Klarna badges removed from `FooterIcons.tsx` (`TrustBadges` now shows DHL + a text "Invoice · Bank transfer" / "Rechnung · Überweisung"). Icon files kept in `assets/icons/`.
- **D14 — 18+ gate.** `src/features/age/age.ts` (`wecare.ageConfirmed`, device-level, not session-cleared) + `AgeGate` shown once before the assessment (`AssessmentEnginePage` early-returns it until confirmed). DOB-at-registration is a backend/D24 task (no registration form yet). **LEGAL REVIEW REQUIRED** for sufficiency.
- **D16 — data minimisation.** `src/lib/analytics.ts` now: strips the raw answer value from `assessment_question_answered` (question id + index only); documents the "no health data / no PII to vendors" rule; adds automatic props `funnel_version` / `device_class` / `language` and a first-touch UTM capture (sessionStorage).
- **D17 / D18 — analytics.** Target named as **PostHog EU** in `dispatch()` (still empty — needs a key). New events wired: `homepage_cta_clicked` (hero + final CTA), `problem_page_cta_clicked` (landing CTAs), `assessment_back_clicked`, `medical_review_submitted`, `recommendation_cta_clicked target=alternative`. `.env.example` added (`VITE_POSTHOG_*`, `VITE_ERROR_DSN`, `VITE_SITE_ORIGIN`).
- **D19 — error reporting.** `RootErrorBoundary.componentDidCatch` comment names GlitchTip (EU/Sentry-compatible) + the scrubbing rules; `VITE_ERROR_DSN` seam.
- **D24 — backend architecture** captured in `docs/BACKEND-ARCHITECTURE.md` (EU residency, the D3 flow, services, entities, the 3–5 week MVP estimate caveat).
- **D26 — the six prior changes: all approved.** #5 (deleted review page) → rebuilt per D3 (above). Everything else stays.

**Still owner/partner/counsel-blocked after this round:** D4 (real SLA), D5 (review fee), D6 (real prices), D8 (shipping economics), D9/D10/D11 (entity + partner data + real COA), D12/D15 (counsel), D20 (real domain), D21 (SEO go-live timing + OG image), D22 (real support contact), D24 (the backend build itself), D25 (mobile device pass). D13 (buy Usercentrics), D17 (PostHog EU account), D19 (GlitchTip account) need procurement.

Flag anything in the codebase that conflicts with the spec instead of silently working around it.
